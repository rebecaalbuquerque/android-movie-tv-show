<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.albuquerque.tvshow">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".core.application.TvShowApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".modules.auth.view.activity.AuthActivity"
            android:theme="@style/AuthActivityTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".modules.shows.view.activity.HomeActivity" />
        <activity android:name=".modules.auth.view.activity.SettingsActivity" />
        <activity
            android:label=""
            android:name=".modules.shows.view.activity.DetailActivity"
            android:parentActivityName=".modules.shows.view.activity.HomeActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.albuquerque.tvshow.modules.shows.view.activity.HomeActivity" />
        </activity>
    </application>

</manifest>